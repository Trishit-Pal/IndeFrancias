# Dependencies / build output (frenchpath/ has its own .gitignore too)
node_modules/
frenchpath/node_modules/
frenchpath/build/
frenchpath/.svelte-kit/
frenchpath/coverage/
frenchpath/test-results/
frenchpath/playwright-report/

# OS
.DS_Store
Thumbs.db

# Session artifacts (design critiques, not versioned docs)
.impeccable/

# Env / secrets
.env
.env.*
!.env.example
